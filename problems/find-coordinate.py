# https://programmers.co.kr/tryouts/3929/challenges/11616
from typing import List

findHypotenuseVertices = lambda v : [v1 for i, v1 in enumerate(v) for v2 in v[:i] + v[i:] if v1[0] != v2[0] and v1[1] != v2[1] ]
findMidPoint = 
def solution(v):
    hypotenuseVertices = findHypo
    print(hypotenuseVertices)
    
    answer = []

    return answer
